   conda deactivate
   uv python list
   python3 version
   python --version
   uv pip list
   pip install -r requirements.txt
   clear
   touch agent
   mkdir agent
   mkdir config
   cd config
   touch config.yaml
   cd ..
   clear
   doskey/history
   clear